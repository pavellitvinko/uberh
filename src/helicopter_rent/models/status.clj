(ns helicopter_rent.models.status
  (:gen-class))

(def status {:submitted "submitted" 
             :canceled "canceled"
             :completed "completed"
             :in_progress "in_progress"})
